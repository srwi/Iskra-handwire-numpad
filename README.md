KeyPeek layer/key HID notifications are provided by the external module:
https://github.com/srwi/zmk-keypeek-layer-notifier

If the keyboard has been paired via Bluetooth before enabling raw HID support, re-pair it so the host detects HID events correctly.
